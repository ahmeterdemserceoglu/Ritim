import{useEffect,useState}from'react';import{ActivityIndicator,FlatList,Pressable,StyleSheet,Text,View}from'react-native';import{useLocalSearchParams,useRouter}from'expo-router';import{colors}from'@/theme';import{searchAllMusic}from'@/services/search';import{TrackRow}from'@/components/TrackRow';
export default function Genre(){const r=useRouter();const{name}=useLocalSearchParams<{name:string}>();const[tracks,setTracks]=useState<any[]>([]);const[loading,setLoading]=useState(true);useEffect(()=>{if(!name)return;searchAllMusic(name).then(x=>setTracks(x.filter(t=>!!t.streamUrl))).finally(()=>setLoading(false))},[name]);return <View style={s.c}><View style={s.top}><Pressable onPress={()=>r.back()}><Text style={s.back}>‹</Text></Pressable><View style={{flex:1}}><Text style={s.kicker}>TÜR</Text><Text style={s.h}>{name}</Text></View></View>{loading?<ActivityIndicator color={colors.green} style={{marginTop:70}}/>:<FlatList data={tracks} keyExtractor={(x,i)=>`${x.id}-${i}`} renderItem={({item})=><TrackRow track={item} queue={tracks}/>} contentContainerStyle={{padding:16,paddingBottom:100}} ListEmptyComponent={<Text style={s.empty}>Bu tür için oynatılabilir açık kaynak bulunamadı.</Text>}/>}</View>}
const s=StyleSheet.create({c:{flex:1,backgroundColor:colors.bg},top:{paddingTop:56,paddingHorizontal:16,paddingBottom:12,flexDirection:'row',alignItems:'center',gap:12},back:{color:colors.text,fontSize:34},kicker:{color:colors.green,fontSize:10,fontWeight:'900',letterSpacing:2},h:{color:colors.text,fontSize:28,fontWeight:'900',textTransform:'capitalize'},empty:{color:colors.muted,textAlign:'center',marginTop:70}});
