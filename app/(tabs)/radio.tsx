import{useEffect,useState}from"react";import{ActivityIndicator,FlatList,Pressable,StyleSheet,Text,TextInput,View}from"react-native";import{colors}from"@/theme";import{searchStations}from"@/services/radioBrowser";import type{RadioStation}from"@/types/music";import{usePlayer}from"@/context/PlayerContext";
export default function Radio(){const[q,setQ]=useState("Turkey");const[items,setItems]=useState<RadioStation[]>([]);const[loading,setLoading]=useState(true);const{playRadio}=usePlayer();const load=async(v=q)=>{setLoading(true);try{setItems(await searchStations(v))}finally{setLoading(false)}};useEffect(()=>{load("Turkey")},[]);return <View style={s.c}><Text style={s.h}>Canlı Radyo</Text><Text style={s.sub}>Dünyadan ücretsiz radyo istasyonları.</Text><TextInput value={q} onChangeText={setQ} onSubmitEditing={()=>load()} placeholder="İstasyon ara" placeholderTextColor={colors.muted} style={s.i}/>{loading?<ActivityIndicator color={colors.green} style={{marginTop:40}}/>:<FlatList data={items} keyExtractor={x=>x.id} contentContainerStyle={{paddingBottom:140}} renderItem={({item})=><Pressable onPress={()=>playRadio(item.name,item.streamUrl,item.favicon)} style={s.row}><View style={{flex:1}}><Text style={s.n}>{item.name}</Text><Text style={s.m}>{item.country||"Radyo"}</Text></View><Text style={s.play}>▶</Text></Pressable>}/>}</View>}
const s=StyleSheet.create({c:{flex:1,backgroundColor:colors.bg,paddingTop:65,paddingHorizontal:16},h:{color:colors.text,fontSize:31,fontWeight:"900"},sub:{color:colors.muted,marginTop:5,marginBottom:16},i:{height:50,borderRadius:15,backgroundColor:colors.card,color:colors.text,paddingHorizontal:15,borderWidth:1,borderColor:colors.line,marginBottom:12},row:{minHeight:68,flexDirection:"row",alignItems:"center",borderBottomWidth:1,borderBottomColor:colors.line},n:{color:colors.text,fontWeight:"800",fontSize:15},m:{color:colors.muted,fontSize:12,marginTop:3},play:{color:colors.green,fontSize:18,padding:12}});
