import React,{createContext,useContext,useEffect,useMemo,useState} from "react";
import {setAudioModeAsync,useAudioPlayer,useAudioPlayerStatus} from "expo-audio";
import type {Track} from "@/types/music";
type C={currentTrack:Track|null;queue:Track[];isPlaying:boolean;currentTime:number;duration:number;playTrack:(t:Track,q?:Track[])=>void;playRadio:(title:string,url:string,art?:string)=>void;toggle:()=>void;seekTo:(s:number)=>Promise<void>;next:()=>void;previous:()=>void};
const Context=createContext<C|null>(null);
export function PlayerProvider({children}:{children:React.ReactNode}){const player=useAudioPlayer(null,{updateInterval:500});const status=useAudioPlayerStatus(player);const[currentTrack,setCurrentTrack]=useState<Track|null>(null);const[queue,setQueue]=useState<Track[]>([]);useEffect(()=>{setAudioModeAsync({playsInSilentMode:true,shouldPlayInBackground:true,interruptionMode:"doNotMix"}).catch(()=>{})},[]);const playTrack=(t:Track,q?:Track[])=>{if(!t.streamUrl)return;if(q)setQueue(q);setCurrentTrack(t);player.replace(t.streamUrl);player.setActiveForLockScreen(true,{title:t.title,artist:t.artist,albumTitle:t.album,artworkUrl:t.artworkUrl});player.play()};const playRadio=(title:string,url:string,art?:string)=>playTrack({id:`radio:${title}:${url}`,source:"openverse",title,artist:"Canlı Radyo",streamUrl:url,artworkUrl:art});const toggle=()=>{if(!currentTrack)return;status.playing?player.pause():player.play()};const seekTo=async(s:number)=>player.seekTo(Math.max(0,s));const move=(d:number)=>{if(!currentTrack||!queue.length)return;const i=queue.findIndex(x=>x.id===currentTrack.id);playTrack(queue[i<0?0:(i+d+queue.length)%queue.length],queue)};const value=useMemo(()=>({currentTrack,queue,isPlaying:!!status.playing,currentTime:status.currentTime||0,duration:status.duration||0,playTrack,playRadio,toggle,seekTo,next:()=>move(1),previous:()=>move(-1)}),[currentTrack,queue,status.playing,status.currentTime,status.duration]);return <Context.Provider value={value}>{children}</Context.Provider>}
export function usePlayer(){const v=useContext(Context);if(!v)throw new Error("usePlayer, PlayerProvider içinde kullanılmalı.");return v}
